# Serve the compiled React application directly from the private IRIS web server.
<Location /meuportal/>
    CSP Off
</Location>

# Keep the ObjectScript REST API routed through the IRIS Web Gateway.
<Location /meuportal/api/>
    CSP On
</Location>

RedirectMatch 302 ^/meuportal/$ /meuportal/index.html
